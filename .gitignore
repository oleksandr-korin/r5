# build
dist/
.astro/

# deps
node_modules/

# env
.env
.env.*

# logs
*.log

# os
.DS_Store

# Original design handoff zip — extracted into r5-website/ which is committed.
R5 Website Handoff.zip

# Source media in the repo root — the optimized versions live in public/ and
# are what ships. Keep originals local for re-encoding without committing tens
# of MB of raw assets.
/Screenshot*.png
/Эпизод*.mp4
/hero-bg.png
